// Must match PLATFORM_FEE_RATE in server/routes/payments.py — display-only
// here, the server is the authoritative source for the actual charge.
export const PLATFORM_FEE_RATE = 0.10
export const PLATFORM_FEE_PERCENT_LABEL = '10%'
