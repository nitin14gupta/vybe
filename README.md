now i am plaaning to add a host onboarding so when a person for the first time so this iwll b in the datbase stored for every user has tehy completed the host onboarding so as click + icon or create event check if true then good they can create evnet if not then showing the host onboarding. so the ui prt you 