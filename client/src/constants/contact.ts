// Single source of truth for the app's support/contact email — every screen
// that shows or mailto:s a support address should import from here, so a
// domain change only means editing this one line.
export const SUPPORT_EMAIL = 'support@gorave.com'
